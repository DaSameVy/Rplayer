# Rplayer

Rplayer is an applation to play videos with Gstreamer as backend and Tauri as frontend. To use the app, you should install Gstreamer at first.

Use the following command to build the app
```
npm run tauri build
```